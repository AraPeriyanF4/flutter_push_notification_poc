name: notify
description: Flutter app to demonstrate FCM notifications.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  overlay_support: ^2.1.0
  firebase_messaging: ^14.1.3
  firebase_core: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
